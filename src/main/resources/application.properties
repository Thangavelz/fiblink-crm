spring.application.name=fiblink-crm


# Server
server.port=8080

# Datasource
spring.datasource.url=jdbc:postgresql://localhost:5432/fiblink-crm-db
spring.datasource.username=postgres
spring.datasource.password=password

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

ring.jpa.hibernate.ddl-auto=update
telegram.bot.token=7550247476:AAFf9XDcqSKoMpnpS9BZ-N5z82yp7Ns-rmM
 
telegram.admin.group.chatId=-1002742654722
spring.jackson.deserialization.fail-on-unknown-properties=false


# ─── JWT ──────────────────────────────────────────────────────────────────────
# PRODUCTION: override with a secure random 64-char string via env variable
#   export APP_JWT_SECRET=<random>
# or in docker-compose:
#   environment:
#     APP_JWT_SECRET: ${JWT_SECRET}
app.jwt.secret=fiblink-crm-super-secret-key-2024-change-me!!
 
# ─── CORS ─────────────────────────────────────────────────────────────────────
# Development (all localhost ports)
app.cors.allowed-origins=http://localhost:*
 
# Production: replace with your exact frontend URL (no trailing slash)
# app.cors.allowed-origins=https://crm.fiblink.com
 
# ─── Cookie notes ─────────────────────────────────────────────────────────────
# In AuthController, ResponseCookie has .secure(false) for local dev.
# For production set .secure(true) and deploy behind HTTPS.